# AIRCTRL ARCHITECTURE DIAGRAM (Day-5)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         AIRCTRL FULL PIPELINE                                │
│                   (Perception → Vocabulary → Context → Action)               │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ DAY-1: PERCEPTION LAYER (Computer Vision)                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│  Webcam Input → MediaPipe HandTracker → 21 Hand Landmarks (x, y, z)         │
│  Files: src/camera/webcam.py, src/vision/hand_tracker.py                    │
└─────────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│ DAY-2: DETECTION LAYER (Finger State Analysis)                              │
├─────────────────────────────────────────────────────────────────────────────┤
│  Landmarks → Finger States (UP/DOWN) → Finger Count                          │
│  Example: [Thumb: UP, Index: UP, Middle: DOWN, Ring: DOWN, Pinky: DOWN]     │
│  Files: src/core/gesture_logic.py                                           │
└─────────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│ DAY-3: VOCABULARY LAYER (Gesture Recognition)                               │
├─────────────────────────────────────────────────────────────────────────────┤
│  Finger States → GestureSignal (Symbolic Name)                              │
│  Example: 2 fingers up → GestureSignal(name="TWO_FINGERS")                  │
│  Files: src/core/gesture_vocabulary.py, src/core/gesture_signal.py          │
└─────────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│ DAY-4: CONTEXT AWARENESS LAYER (Intent Mapping)                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  GestureSignal + Active Context → ContextSignal (User Intent)               │
│  Example: TWO_FINGERS + BROWSER → ContextSignal(intent="SCROLL_UP")         │
│  Files: src/core/context_detector.py, src/core/context_mapper.py,           │
│         src/core/context_signal.py                                           │
└─────────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│ DAY-5: ACTION EXECUTION LAYER (OS Control) ★ NEW ★                          │
├─────────────────────────────────────────────────────────────────────────────┤
│  ContextSignal → ActionSignal → Execute (if enabled)                        │
│  Example: ContextSignal(intent="SCROLL_UP")                                 │
│           → ActionSignal(intent="SCROLL_UP", action="scroll_up")            │
│           → execute_action() → pyautogui.scroll(5)                          │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────┐            │
│  │ EXECUTION GATE (Safety Control)                             │            │
│  ├─────────────────────────────────────────────────────────────┤            │
│  │ Enabled (🟢)  → Actions execute                             │            │
│  │ Disabled (🔴) → Actions blocked                             │            │
│  │ Toggle: Press 'E' key                                        │            │
│  │ Emergency Stop: Move mouse to screen corner (FAILSAFE)      │            │
│  └─────────────────────────────────────────────────────────────┘            │
│                                                                              │
│  Files: src/core/action_signal.py, src/core/action_executor.py              │
└─────────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│ OS OPERATIONS (Windows API via pyautogui)                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  • Mouse wheel scroll (up/down)                                             │
│  • Keyboard shortcuts (Ctrl+, Volume keys)                                  │
│  • System controls (volume, zoom)                                           │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## DATA FLOW EXAMPLE: Two-Finger Scroll in Browser

```
Step 1: PERCEPTION (Day-1)
   Webcam → MediaPipe → 21 hand landmarks
   
Step 2: DETECTION (Day-2)
   Landmarks → Finger States: [Thumb: ↓, Index: ↑, Middle: ↑, Ring: ↓, Pinky: ↓]
                             → Fingers Up: 2
   
Step 3: VOCABULARY (Day-3)
   Finger States (2 up) → GestureSignal(name="TWO_FINGERS")
   
Step 4: CONTEXT AWARENESS (Day-4)
   Active Window: "Google Chrome"
                → Context: "BROWSER"
   Gesture "TWO_FINGERS" + Context "BROWSER"
                → ContextSignal(gesture="TWO_FINGERS", 
                                context="BROWSER", 
                                intent="SCROLL_UP")
   
Step 5: ACTION EXECUTION (Day-5) ★ NEW ★
   ContextSignal(intent="SCROLL_UP")
                → intent_to_action()
                → ActionSignal(intent_name="SCROLL_UP", 
                               action_name="scroll_up")
                → execute_action()
                → Check: is_execution_enabled()?
                   ├─ YES (🟢) → pyautogui.scroll(5) → Browser scrolls up
                   └─ NO  (🔴) → Log "Action blocked" → No operation
```

---

## SAFETY ARCHITECTURE

```
┌─────────────────────────────────────────────────────────────────────┐
│                      SAFETY CONTROL POINTS                          │
└─────────────────────────────────────────────────────────────────────┘

1. GLOBAL EXECUTION FLAG (Default: DISABLED)
   ┌──────────────────────────────────────┐
   │ _execution_enabled = False           │
   │                                      │
   │ Controls: enable_execution()         │
   │           disable_execution()        │
   │           is_execution_enabled()     │
   └──────────────────────────────────────┘

2. PYAUTOGUI FAILSAFE (Always Active)
   ┌──────────────────────────────────────┐
   │ pyautogui.FAILSAFE = True            │
   │                                      │
   │ Mouse to screen corner → Exception   │
   │ → Auto-disable execution             │
   └──────────────────────────────────────┘

3. ACTION VALIDATION (Every Execution)
   ┌──────────────────────────────────────┐
   │ execute_action(action_signal):       │
   │   if not _execution_enabled:         │
   │     log("blocked") & return          │
   │   if action == "no_action":          │
   │     return                           │
   │   try:                               │
   │     perform_os_operation()           │
   │   except FailSafeException:          │
   │     disable_execution()              │
   └──────────────────────────────────────┘

4. RUNTIME TOGGLE (User Control)
   ┌──────────────────────────────────────┐
   │ Press 'E' key:                       │
   │   if enabled:                        │
   │     disable_execution()              │
   │   else:                              │
   │     enable_execution()               │
   └──────────────────────────────────────┘
```

---

## MODULE DEPENDENCY GRAPH

```
main.py
  ├─→ camera/webcam.py
  ├─→ vision/hand_tracker.py
  ├─→ core/gesture_logic.py
  ├─→ core/gesture_vocabulary.py
  │     └─→ core/gesture_signal.py
  ├─→ core/context_detector.py
  ├─→ core/context_mapper.py
  │     ├─→ core/gesture_signal.py
  │     └─→ core/context_signal.py
  └─→ core/action_executor.py ★ NEW ★
        └─→ core/action_signal.py ★ NEW ★

External Dependencies:
  • opencv-python (cv2)
  • mediapipe
  • numpy
  • pyautogui ★ NEW (Day-5) ★
```

---

## INTENT-ACTION MAPPING TABLE

```
┌────────────────┬─────────────┬────────────┬──────────────────────────┐
│ Context        │ Gesture     │ Intent     │ Action (OS Operation)    │
├────────────────┼─────────────┼────────────┼──────────────────────────┤
│ BROWSER        │ TWO_FINGERS │ SCROLL_UP  │ pyautogui.scroll(5)      │
│ BROWSER        │ POINT       │ SCROLL_DOWN│ pyautogui.scroll(-5)     │
│ DOCUMENT       │ TWO_FINGERS │ ZOOM       │ pyautogui.hotkey('ctrl',│
│                │             │            │                     '=') │
│ MEDIA          │ TWO_FINGERS │ VOLUME     │ pyautogui.press(         │
│                │             │            │            'volumeup')   │
│ (any)          │ FIST        │ PAUSE      │ (not implemented)        │
│ (any)          │ OPEN_PALM   │ HOME       │ (not implemented)        │
│ (any)          │ UNKNOWN     │ NONE       │ (no action)              │
└────────────────┴─────────────┴────────────┴──────────────────────────┘

Legend:
  ✓ Implemented (Day-5)
  ⚠ Mapped but not executed (Day-6+)
```

---

## CONSOLE OUTPUT ANATOMY

```
Right Hand | Gesture: TWO_FINGERS  | Context: BROWSER    | Intent: SCROLL_UP     | Action: scroll_up     | Exec: 🟢 | FPS: 28.5
    │           │                       │                     │                       │                  │       │
    │           │                       │                     │                       │                  │       └─ Performance
    │           │                       │                     │                       │                  │
    │           │                       │                     │                       │                  └─ Execution Status
    │           │                       │                     │                       │                     🟢 = Enabled
    │           │                       │                     │                       │                     🔴 = Disabled
    │           │                       │                     │                       │
    │           │                       │                     │                       └─ DAY-5: Concrete Action
    │           │                       │                     │                          (Infrastructure Layer)
    │           │                       │                     │
    │           │                       │                     └─ DAY-4: User Intent
    │           │                       │                        (Domain Layer)
    │           │                       │
    │           │                       └─ DAY-4: Active Window Context
    │           │                          (Environment Detection)
    │           │
    │           └─ DAY-3: Symbolic Gesture Name
    │              (Vocabulary Translation)
    │
    └─ DAY-1: Hand Detection
       (Perception)
```

---

## EXECUTION STATE MACHINE

```
┌───────────────┐
│  SYSTEM START │
└───────┬───────┘
        │
        ↓
┌───────────────────┐
│ DISABLED (🔴)     │ ←─────────────┐
│ Default State     │                │
│ Actions: BLOCKED  │                │
└───────┬───────────┘                │
        │                            │
        │ Press 'E'                  │ Emergency Stop
        ↓                            │ (Failsafe)
┌───────────────────┐                │
│ ENABLED (🟢)      │                │
│ Warning Shown     │                │
│ Actions: EXECUTE  │ ───────────────┘
└───────┬───────────┘
        │
        │ Press 'E'
        ↓
┌───────────────────┐
│ DISABLED (🔴)     │
│ Confirmation      │
│ Actions: BLOCKED  │
└───────────────────┘
        │
        │ (loop continues)
        ↓
```

---

## FILE SIZE & COMPLEXITY METRICS

```
Module                      Lines    Purpose                      Day
────────────────────────────────────────────────────────────────────────
src/main.py                  200     Main loop orchestration      1-5
src/camera/webcam.py         150     Camera capture               1
src/vision/hand_tracker.py   200     MediaPipe integration        1
src/core/gesture_logic.py    180     Finger state detection       2
src/core/gesture_signal.py    80     Gesture data model           3
src/core/gesture_vocabulary  220     Gesture recognition          3
src/core/context_detector.py 150     Window context detection     4
src/core/context_signal.py    80     Intent data model            4
src/core/context_mapper.py   220     Intent mapping rules         4
src/core/action_signal.py     90     Action data model            5 ★
src/core/action_executor.py  260     Safe OS control execution    5 ★
────────────────────────────────────────────────────────────────────────
TOTAL                       1,830    Full pipeline               1-5
```

---

**Architecture Status**: COMPLETE (Days 1-5)
**Safety Status**: ✅ All checks passed
**Next Phase**: Day-6 – Gesture Smoothing & Temporal Filtering
